---
title: BirdNET App – Identify Birds by Sound
layout: layouts/base.njk
permalink: "/app/"
---

<section class="page-header">
  <div class="container">
    <h1 class="page-title">BirdNET App</h1>
    <p class="lead text-muted mb-0">
      Identify birds around you by recording a short snippet of song or call. Fast, free, and powered by BirdNET AI.
    </p>
    <!-- BirdNET app header -->
    <div class="mt-5 text-left">
      <img
        src="{{ '/img/app_header.jpg' | url }}"
        alt="BirdNET app header"
        class="img-fluid rounded shadow-sm"
        style="max-height: 400px; width: auto;">
    </div>
  </div>
</section>

<section class="py-5">
  <div class="container">

    <!-- Download buttons -->
    <div class="mb-5">
      <a href="https://play.google.com/store/apps/details?id=de.tu_chemnitz.mi.kahst.birdnet"
         class="btn btn-dark btn-sm me-2" target="_blank" rel="noopener">
        Android (Google Play) ↗
      </a>
      <a href="https://apps.apple.com/us/app/birdnet/id1541842885"
         class="btn btn-dark btn-sm" target="_blank" rel="noopener">
        iOS (App Store) ↗
      </a>
    </div>

    <!-- Live BirdNET app stats -->
    <div id="network-stats" class="mb-5">
      <h2 class="h5 section-title mb-3">BirdNET app activity</h2>
      <div class="row g-3 align-items-stretch text-center">
        <div class="col-6 col-md-3">
          <div class="card shadow-sm h-100">
            <div class="card-body py-3">
              <div class="small text-muted">Total observations</div>
              <div id="stat-total-observations" class="h4 mb-0 fw-semibold">—</div>
            </div>
          </div>
        </div>
        <div class="col-6 col-md-3">
          <div class="card shadow-sm h-100">
            <div class="card-body py-3">
              <div class="small text-muted">Observations (24h)</div>
              <div id="stat-observations-24h" class="h4 mb-0 fw-semibold">—</div>
            </div>
          </div>
        </div>
        <div class="col-6 col-md-3">
          <div class="card shadow-sm h-100">
            <div class="card-body py-3">
              <div class="small text-muted">Species (24h)</div>
              <div id="stat-species-24h" class="h4 mb-0 fw-semibold">—</div>
            </div>
          </div>
        </div>
        <div class="col-6 col-md-3">
          <div class="card shadow-sm h-100">
            <div class="card-body py-3">
              <div class="small text-muted">Users (24h)</div>
              <div id="stat-users-24h" class="h4 mb-0 fw-semibold">—</div>
            </div>
          </div>
        </div>
      </div>
      <div id="stats-updated" class="small text-muted mt-2">Loading…</div>
    </div>    

    <div class="row g-4 mb-5">
      <div class="col-lg-6">
        <h2 class="h5 section-title">Overview</h2>
        <p class="text-muted">
          The BirdNET mobile app lets you hold up your phone, record a few seconds of sound, and get instant
          identification suggestions. It uses the same core neural network models that power research-grade
          monitoring tools, simplified for everyday use.
        </p>
        <ul class="text-muted small mb-3">
          <li>Real-time bird sound identification.</li>
          <li>Confidence scores and alternative suggestions.</li>
          <li>Optional contribution of observations for research.</li>
          <li>Works in many regions worldwide (model updates expand coverage).</li>
        </ul>
        <p class="text-muted small mb-0">
          Designed for birders, learners, educators, and anyone curious about the sounds they hear outdoors.
        </p>
      </div>
      <div class="col-lg-6">
        <div class="card shadow-sm h-100">
          <div class="card-body">
            <img src="{{ '/img/birdnet_app_screenshots.png' | url }}"
                 alt="BirdNET app interface screenshots"
                 class="img-fluid rounded">
            <p class="small text-muted mt-2 mb-0">Representative interface screenshots.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Features -->
    <div class="row g-4 mb-5">
      <div class="col-md-4">
        <div class="card h-100 shadow-sm">
          <div class="card-body">
            <h3 class="h6 text-uppercase text-muted mb-2">Simple capture</h3>
            <p class="small text-muted mb-0">Tap record, wait for the birds to call, select a clip, and submit for analysis. No external microphone needed.</p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card h-100 shadow-sm">
          <div class="card-body">
            <h3 class="h6 text-uppercase text-muted mb-2">Instant results</h3>
            <p class="small text-muted mb-0">Top species suggestions with confidence help you learn local birds faster.</p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card h-100 shadow-sm">
          <div class="card-body">
            <h3 class="h6 text-uppercase text-muted mb-2">Learning tools</h3>
            <p class="small text-muted mb-0">Link detections to species info and improve recognition through repetition.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- How it works -->
    <div class="mb-5">
      <h2 class="h5 section-title">How it works</h2>
      <div class="row g-4">
        <div class="col-sm-6 col-lg-3">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <h3 class="h6 text-muted text-uppercase mb-2">1. Record</h3>
              <p class="small text-muted mb-0">Phone microphone captures ambient sound (short clip).</p>
            </div>
          </div>
        </div>
        <div class="col-sm-6 col-lg-3">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <h3 class="h6 text-muted text-uppercase mb-2">2. Analyze</h3>
              <p class="small text-muted mb-0">Clip converted to a spectrogram; BirdNET model scores species.</p>
            </div>
          </div>
        </div>
        <div class="col-sm-6 col-lg-3">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <h3 class="h6 text-muted text-uppercase mb-2">3. Suggest</h3>
              <p class="small text-muted mb-0">Top matches returned with confidence and alternate options.</p>
            </div>
          </div>
        </div>
        <div class="col-sm-6 col-lg-3">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <h3 class="h6 text-muted text-uppercase mb-2">4. Learn</h3>
              <p class="small text-muted mb-0">Users explore species info, improving recognition over time.</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Data & privacy -->
    <div class="row g-4 mb-5">
      <div class="col-lg-6">
        <h2 class="h5 section-title">Data & privacy</h2>
        <p class="text-muted small mb-2">
          Your recordings help improve BirdNET. Submissions are used for research (e.g., to train and evaluate models)
          and are licensed under
          <a href="https://creativecommons.org/licenses/by-sa/4.0/" target="_blank" rel="noopener">CC BY‑SA 4.0</a>.
        </p>
        <ul class="small text-muted mb-3">
          <li>Human speech is filtered; we do not publish clips containing speech. Published examples are anonymized.</li>
          <li>Location is optional and user‑controlled; when provided, it helps track species diversity and change over time.</li>
          <li>Data are stored securely to prevent unauthorized access.</li>
          <li>Used for non‑commercial research and model development only.</li>
        </ul>
        <p class="small text-muted mb-0">
          See the in‑app policy screen for additional details.
        </p>
      </div>
      <div class="col-lg-6">
        <h2 class="h5 section-title">Tips</h2>
        <ul class="small text-muted mb-0">
          <li>Move closer to the singing bird when possible.</li>
          <li>Avoid handling noise (hold device steady while recording).</li>
          <li>Limit overlapping sounds (traffic, voices) for better accuracy.</li>
          <li>Try multiple short recordings if the first is uncertain.</li>
        </ul>
      </div>
    </div>

    <!-- BirdNET Live (PWA) Promo -->
    <div class="card shadow-sm border-primary mb-5">
      <div class="card-body p-4">
        <div class="row align-items-center">
          <div class="col-lg-8">
            <h2 class="h5 text-primary mb-2">
              BirdNET Live (Web App)
            </h2>
            <p class="mb-2">
              Identify birds in real-time directly in your browser. <strong>BirdNET Live</strong> is a Progressive Web App (PWA) that processes sound locally on your device using TensorFlow.js.
            </p>
            <div class="row g-2 small text-muted mb-3">
              <div class="col-sm-6">
                <ul class="list-unstyled mb-0">
                  <li class="mb-1"><strong>Run offline:</strong> No audio uploaded, works without internet.</li>
                  <li class="mb-1"><strong>Real-time:</strong> Instant spectrogram & predictions.</li>
                </ul>
              </div>
              <div class="col-sm-6">
                <ul class="list-unstyled mb-0">
                  <li class="mb-1"><strong>Cross-platform:</strong> Desktop & mobile browsers.</li>
                  <li class="mb-1"><strong>Location-aware:</strong> Filters by local species.</li>
                </ul>
              </div>
            </div>
            <p class="small text-muted mb-0 fst-italic">
              Note: This project is in active development. Features and performance may vary.
            </p>
          </div>
          <div class="col-lg-4 text-center text-lg-end mt-3 mt-lg-0">
            <a href="https://birdnet-team.github.io/real-time-pwa/" class="btn btn-primary mb-2" target="_blank" rel="noopener">
              Launch BirdNET Live ↗
            </a>
            <div class="small text-muted">
              No install required.<br>Add to home screen for app-like experience.
            </div>
          </div>
        </div>
      </div>
    </div>


    <!-- Support -->
    <div class="row g-4 mb-4">
      <div class="col-lg-8">
        <h2 class="h5 section-title">Support & feedback</h2>
        <p class="small text-muted mb-2">
          If you encounter issues or have improvement suggestions, please use the feedback option in the app or contact the team.
        </p>
        <p class="small text-muted mb-0">
          Email: <a href="mailto:ccb-birdnet@cornell.edu">ccb-birdnet@cornell.edu</a>
        </p>
      </div>
      <div class="col-lg-4">
        <div class="card h-100 shadow-sm">
          <div class="card-body">
            <h3 class="h6 text-uppercase text-muted mb-2">Get the app</h3>
            <a href="https://play.google.com/store/apps/details?id=de.tu_chemnitz.mi.kahst.birdnet"
               class="btn btn-dark btn-sm w-100 mb-2" target="_blank" rel="noopener">
              Android ↗
            </a>
            <a href="https://apps.apple.com/us/app/birdnet/id1541842885"
               class="btn btn-dark btn-sm w-100" target="_blank" rel="noopener">
              iOS ↗
            </a>
            <p class="small text-muted mt-2 mb-0">Free download. Requires microphone access.</p>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<!-- Stats fetch script (moved to very end, after all DOM elements) -->
<script>
(function(){
  const STATS_URL = 'https://birdnet.cornell.edu/api2/requeststats';
  const REFRESH_MS = 5 * 60 * 1000;
  let refreshTimer = null;

  function fmtInt(v){
    return (typeof v === 'number' && isFinite(v)) ? v.toLocaleString('en-US') : '0';
  }

  async function fetchStats(){
    try {
      const res = await fetch(STATS_URL, { cache: 'no-store', credentials: 'omit' });
      if(!res.ok){
        throw new Error('HTTP '+res.status+' '+res.statusText);
      }
      const data = await res.json();
      processStats(data);
    } catch (e){
      console.error('Stats fetch failed:', e);
      const el = document.getElementById('stats-updated');
      if(el) el.textContent = 'Failed to load stats.';
    }
  }

  function processStats(data){
    const observationsRaw = Array.isArray(data.observations) ? data.observations : [];
    const observationsSorted = observationsRaw.slice().sort((a,b)=>(+b.ts)-(+a.ts));
    const now = Date.now();
    const dayAgo = now - 24*3600*1000;
    const obs24h = observationsSorted.filter(o => +o.ts >= dayAgo);
    const statsObj = (data.stats && typeof data.stats === 'object') ? data.stats : {};

    const totalObservations = statsObj.total_observations ?? observationsSorted.length;
    const observations24h = statsObj.observations ?? obs24h.length;
    const users24h = statsObj.users ?? 0;

    const speciesSet = new Set();
    for (const o of obs24h){
      if(typeof o.species === 'string'){
        const common = o.species.split(';')[0] || o.species;
        speciesSet.add(common);
      }
    }
    const species24h = speciesSet.size;

    // Guard each update in case IDs were reordered/removed
    const setText = (id,val)=> {
      const el = document.getElementById(id);
      if(el) el.textContent = val;
    };
    setText('stat-total-observations', fmtInt(totalObservations));
    setText('stat-observations-24h', fmtInt(observations24h));
    setText('stat-species-24h', fmtInt(species24h));
    setText('stat-users-24h', fmtInt(users24h));
    setText('stats-updated', 'Updated ' + new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }));

    clearTimeout(refreshTimer);
    refreshTimer = setTimeout(fetchStats, REFRESH_MS);
  }

  function init(){
    fetchStats();
  }

  if(document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
</script>